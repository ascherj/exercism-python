[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.4.2",
    "pytest-cache>=1.0",
    "pytest-pylint>=0.21.0",
    "pytest-subtests>=0.14.2",
]
